/**
 * Cria um novo objeto de uma pessoa.
 * @constructor
 * @param {string} name - Nome da pessoa.
 * @param {number} age - Idade de pessoa.
 */
function Person(name, age) {
  this.name = name;
  this.age = age;
}
