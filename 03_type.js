/**
 * Objeto de uma pessoa.
 * @typedef {Object} Person
 * @property {string} name - Nome da pessoa.
 * @property {number} age - Idade da pessoa.
 */

/**
 * The usuário atual.
 * @type {Person}
 */
const currentUser = {
  name: 'Alice',
  age: 25
};

